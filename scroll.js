const observer = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders1")[0].classList.add("fadeInRight")
    }
  })
})

const observer1 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders2")[0].classList.add("fadeInRight")
    }
  })
})

const observer2 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders3")[0].classList.add("fadeInRight")
    }
  })
})

const observer3 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders4")[0].classList.add("fadeInRight")
    }
  })
})

const observer4 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders5")[0].classList.add("fadeInRight")
    }
  })
})

const observer5 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders6")[0].classList.add("fadeInRight")
    }
  })
})

const observer6 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders7")[0].classList.add("fadeInRight")
    }
  })
})

const observer7 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders8")[0].classList.add("fadeInRight")
    }
  })
})

const observer8 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders9")[0].classList.add("fadeInRight")
    }
  })
})

const observer9 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders10")[0].classList.add("fadeInRight")
    }
  })
})

const observer10 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders11")[0].classList.add("fadeInRight")
    }
  })
})

const observer11 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders12")[0].classList.add("fadeInRight")
    }
  })
})

const observer12 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders13")[0].classList.add("fadeInRight")
    }
  })
})

const observer13 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders14")[0].classList.add("fadeInRight")
    }
  })
})

const observer14 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders15")[0].classList.add("fadeInRight")
    }
  })
})

const observer15 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders16")[0].classList.add("fadeInRight")
    }
  })
})

const observer16 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders17")[0].classList.add("fadeInRight")
    }
  })
})

const observer17 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders18")[0].classList.add("fadeInRight")
    }
  })
})

const observer18 = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      document.querySelectorAll(".faders19")[0].classList.add("fadeInRight")
    }
  })
})




observer.observe(document.querySelector(".faders1"));
observer1.observe(document.querySelector(".faders2"));
observer2.observe(document.querySelector(".faders3"));
observer3.observe(document.querySelector(".faders4"));
observer4.observe(document.querySelector(".faders5"));
observer5.observe(document.querySelector(".faders6"));
observer6.observe(document.querySelector(".faders7"));
observer7.observe(document.querySelector(".faders8"));
observer8.observe(document.querySelector(".faders9"));
observer9.observe(document.querySelector(".faders10"));
observer10.observe(document.querySelector(".faders11"));
observer11.observe(document.querySelector(".faders12"));
observer12.observe(document.querySelector(".faders13"));
observer13.observe(document.querySelector(".faders14"));
observer14.observe(document.querySelector(".faders15"));
observer15.observe(document.querySelector(".faders16"));
observer16.observe(document.querySelector(".faders17"));
observer17.observe(document.querySelector(".faders18"));
observer18.observe(document.querySelector(".faders19"));
